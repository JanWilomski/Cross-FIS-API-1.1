<Window x:Class="Cross_FIS_API_1._1.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Cross_FIS_API_1._1"
        mc:Ignorable="d"
        Title="FIS API Trading Client" Height="600" Width="900"
        WindowStartupLocation="CenterScreen">
    
    <!-- Resources muszą być zdefiniowane PRZED ich użyciem -->
    <Window.Resources>
        <local:BoolToColorConverter x:Key="BoolToColorConverter"/>
    </Window.Resources>
    
    <Window.DataContext>
        <local:MainWindowViewModel />
    </Window.DataContext>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Connection Panel -->
        <GroupBox Grid.Row="0" Header="Connection Settings" Margin="10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                
                <Label Grid.Row="0" Grid.Column="0" Content="Server:" VerticalAlignment="Center"/>
                <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding IpAddress}" Margin="5" Padding="3"/>
                
                <Label Grid.Row="0" Grid.Column="2" Content="Port:" VerticalAlignment="Center"/>
                <TextBox Grid.Row="0" Grid.Column="3" Text="{Binding Port}" Margin="5" Padding="3"/>
                
                <Label Grid.Row="1" Grid.Column="0" Content="User:" VerticalAlignment="Center"/>
                <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding User}" Margin="5" Padding="3"/>
                
                <Label Grid.Row="1" Grid.Column="2" Content="Password:" VerticalAlignment="Center"/>
                <PasswordBox Grid.Row="1" Grid.Column="3" x:Name="PasswordBox" Margin="5" Padding="3" PasswordChanged="PasswordBox_PasswordChanged"/>
                
                <Label Grid.Row="2" Grid.Column="0" Content="Node:" VerticalAlignment="Center"/>
                <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding Node}" Margin="5" Padding="3"/>
                
                <Label Grid.Row="2" Grid.Column="2" Content="Subnode:" VerticalAlignment="Center"/>
                <TextBox Grid.Row="2" Grid.Column="3" Text="{Binding Subnode}" Margin="5" Padding="3"/>
                
                <Button Grid.Row="0" Grid.Column="4" Grid.RowSpan="3" 
                        Content="{Binding ConnectButtonText}" 
                        Command="{Binding ConnectCommand}" 
                        Width="100" Height="30" 
                        Margin="10" VerticalAlignment="Center"
                        Background="{Binding IsConnected, Converter={StaticResource BoolToColorConverter}}"/>
            </Grid>
        </GroupBox>
        
        <!-- Order Panel -->
        <GroupBox Grid.Row="1" Header="Order Management" Margin="10" IsEnabled="{Binding IsConnected}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <Label Grid.Column="0" Content="Symbol:" VerticalAlignment="Center"/>
                <TextBox Grid.Column="1" Text="{Binding OrderSymbol}" Margin="5" Padding="3"/>
                
                <Label Grid.Column="2" Content="Side:" VerticalAlignment="Center"/>
                <ComboBox Grid.Column="3" SelectedValue="{Binding OrderSide}" Margin="5" Padding="3">
                    <ComboBoxItem Content="Buy" Tag="0"/>
                    <ComboBoxItem Content="Sell" Tag="1"/>
                </ComboBox>
                
                <Label Grid.Column="4" Content="Quantity:" VerticalAlignment="Center"/>
                <TextBox Grid.Column="5" Text="{Binding OrderQuantity}" Margin="5" Padding="3"/>
                
                <Button Grid.Column="6" Content="Send Order" Command="{Binding SendOrderCommand}" Margin="10" Padding="10,5"/>
            </Grid>
        </GroupBox>
        
        <!-- Log Panel -->
        <GroupBox Grid.Row="2" Header="Activity Log" Margin="10">
            <ScrollViewer>
                <TextBox Text="{Binding LogText}" 
                         IsReadOnly="True" 
                         TextWrapping="Wrap" 
                         VerticalScrollBarVisibility="Auto"
                         FontFamily="Consolas"
                         Background="Black"
                         Foreground="LightGreen"/>
            </ScrollViewer>
        </GroupBox>
        
        <!-- Status Bar -->
        <StatusBar Grid.Row="3">
            <StatusBarItem>
                <TextBlock Text="{Binding StatusText}"/>
            </StatusBarItem>
            <StatusBarItem HorizontalAlignment="Right">
                <TextBlock Text="{Binding ConnectionStatus}"/>
            </StatusBarItem>
        </StatusBar>
    </Grid>
</Window>